<UserControl x:Class="AntennasCalculator.WpfApp.Controls.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="150" Background="White">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Margin="8">
      <TextBlock x:Name="Title" Text="Fresnel Profile" FontWeight="Bold"/>
      <TextBlock x:Name="Subtitle" Margin="12,0,0,0"/>
    </DockPanel>

    <Grid Grid.Row="1" Margin="8">
      <Canvas x:Name="Canvas" Background="#FAFAFA"/>
      <Border BorderBrush="#DDD" BorderThickness="1"/>
    </Grid>
  </Grid>
</UserControl>